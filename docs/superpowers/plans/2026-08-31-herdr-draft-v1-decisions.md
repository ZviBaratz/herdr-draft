# herdr-draft v1 — decisions log

Every ruling made while executing
`docs/superpowers/plans/2026-08-31-herdr-draft-v1.md` (22 tasks, 43 commits),
in the order made. Each was a call the plan or spec did not settle; each names
what it costs if it was wrong, so it can be found and reversed.

The spec is normative — where a ruling diverges from it, the ruling says so and
the spec is the thing to amend. Two are flagged as spec-amendment candidates:
the reflow-on-focus trade (§6) and the parked path-browsing mode (§6 field 2).

- Ruling: work directly on branch feat/v1 in ~/Projects/herdr-draft, no separate worktree — the repo exists solely for this plan and nothing else runs in it; main stays untouched. Cost if wrong: none material (branch isolation suffices).
- Ruling: `herdrc.CreatedTopology` gains `CheckoutPath string` (populated from the worktree-create response) so `plan.CleanCheck` can hand `gitx.Disposable` a real path — carried into T5 and T13 dispatches. Cost if wrong: small API churn in two packages.
- Ruling: the File Structure dependency note is relaxed per spec §4 (the binding rule is "the form never does I/O"): form may import pure data types from plan/linear/clauth/theme, and must not import herdrc/app/config or perform I/O. Cost if wrong: some import coupling to untangle later.
- Ruling: T22's README uses positional `herdr plugin link <path>` — T2's amended text supersedes the stale `--path` in T22 step 1. Cost if wrong: none.
- Ruling: live probes (T2b, T19) run in a disposable named herdr session, never the user's live session; the clauth probe picks the lowest-utilization profile and does one launch + clean exit. Cost if wrong: brief noise in the user's herdr sidebar / negligible quota use.
- Ruling: Task 1's .gitignore additionally ignores `.superpowers/` (the SDD workspace lives inside this repo and must never be committed). Cost if wrong: none.
Task 4: Ruling: ListBranches(limit<=0) -> empty slice signed off — only planned caller uses 50; defensive-empty on nonsense input is safer than unbounded. Cost if wrong: a future caller must pass a real limit.
Task 9: Ruling: failing api_key_cmd is a hard error (user's chosen key source erroring must surface, not silently degrade to env/literal); empty output falls through. Cost if wrong: a user with a flaky key command sees errors instead of a fallback.
Task 9: Ruling: missing config.toml passes the literal-perm check (the check guards a world-readable stored literal; absent file = literal originated elsewhere). Cost if wrong: negligible.
Task 10: Ruling: state split across recents.json + last-used.json accepted (matches spec §12 file naming). Cost if wrong: trivial refactor.
Task 11: Ruling: Palette fields use image/color.Color, NOT lipgloss.Color — VERIFIED by controller: lipgloss v2.0.5 color.go:68 declares `func Color(s string) color.Color` (a function, no such type). The brief's typing was wrong; deviation accepted and must be carried into T16/T17/T18/T21 dispatches (construct values via lipgloss.Color("#hex"), assign to color.Color fields). Cost if wrong: none — the brief was unbuildable as written.
Task 11: Ruling: auto_switch/"terminal" with unset dark_name falls back to "catppuccin" rather than herdr's sibling-derived default — accepted as the documented best-effort approximation (spec §7 states pixel parity is not a v1 gate; the [palette] config table is the user's escape hatch). Cost if wrong: a themed user on auto_switch sees catppuccin-ish chrome until they set [palette].
Task 12: Ruling: `IsGitRepo bool` added to plan.Input — the brief's prose and T13's brief both require it; only the code sketch omitted it. Cost if wrong: none.
Task 12: Ruling: split direction defaults to "right" (side-by-side) when unspecified — matches herdr's `pane split --direction right|down` vocabulary and the common split idiom; T17's PlacementField may later expose a choice. Cost if wrong: one-line default change.
Task 12: Ruling: folded the untested default-placement branch (a Minor) into fix round 1 rather than deferring — a fix round was already open for the Important finding, the branch is the primary user path (new-space, no worktree), and the cost is one test. Cost if wrong: a few extra tokens.
Task 13: Ruling: busy retry applies to any op kind rather than only the two launch paths — accepted; herdr emits agent_pane_busy only from agent-start/pane-run against a busy pane (src/app/agents.rs:255), so a kind-agnostic guard is behaviourally identical and simpler. Cost if wrong: a future op that legitimately returns that string would retry for 5s.
Task 13: Ruling: promoted the "never panics" doc-overclaim Minor into fix round 1 — the plan's Global Constraints forbid panics on production paths, so the claim is made true (nil guards returning wrapped errors) rather than softened. Cost if wrong: a few defensive branches.
Task 14: Ruling: Picker's list/render/filter reimplemented (Atrium's clean-list picker.go is a key-handling mixin only) — reimplementation is always provenance-safe; accepted.
Task 14: Ruling: generic Picker filters by substring, NOT fuzzy. Atrium's internal/fuzzy is OFF the clean list so it cannot be ported. Spec §6.2 requires fuzzy ranking for the PROJECT picker specifically → carried into Task 17: DirField supplies a freshly-written subsequence ranker (fzf-style, ~30 lines, no Atrium source) via a pluggable match/rank function on Picker. Cost if wrong: directory filtering feels literal until T17 lands the ranker.
Task 14: Ruling: Picker preserves selection by item ID (index-clamp fallback) on same-version SetItems — a background candidate refresh must never silently move the user's selection (spec §6.2's live-updating project scan). Cost if wrong: negligible.
Task 15: Ruling: MapKey takes/returns `armed bool` beyond the brief's literal 2-arg signature — the brief itself requires "arm state passed in/out" for ⌃R⌃R, and threading it explicitly beats hidden mutable state in a pure mapper. Cost if wrong: one call-site signature change in T16.
Task 15: Ruling: ZoneAccount excluded from picker-style Tab-complete accepted — the account field is a pure selection list with no typed fragment to complete (unlike dir/base/issue pickers), so Tab should plainly advance. Cost if wrong: one entry in isPicker().
Task 16: Ruling: `Section.Focus() tea.Cmd` (brief specified no return) — accepted; it is the direct consequence of the carried fact that PromptArea.Focus() emits a blink Cmd that must reach Update. Cost if wrong: none.
Task 16: Ruling: `Section.ID()` + optional capability interfaces (titleValuer/completer/newliner) accepted — MapKey's grammar needs TitleEmpty / picker-complete / prompt-newline signals through an otherwise opaque Section; this is the answer to the "will the interface serve T17/18" question the dispatch asked. Carry into T17/T18: fields implement these capabilities rather than the form special-casing types. Cost if wrong: interface churn in T17/T18.
Task 17: Ruling: WorktreeField exposes three Sections (ChipsSection/BranchSection/BaseSection) rather than being one — accepted; spec §6.4's on-off chips, editable branch and base picker are three independent focus stops in the Tab ring, and Section is one-stop-per-instance. Requirement carried to T20: they must still read as one visual group. Cost if wrong: regrouping in the form's section list.
Task 17: Ruling: shared infra files (lineinput.go, layout.go) beyond the brief's literal file list accepted — ordinary decomposition, not scope creep.
Task 17: Ruling: base-picker rows always rendered rather than focus-gated (unlike DirField) accepted as a disclosed v1 simplification. Cost if wrong: minor visual noise when the base picker is unfocused.
Task 18: Ruling: AgentField derives favorites from the leading entries of SetKinds' own order — accepted. CARRIED REQUIREMENT for T20: the app layer must pass kinds favorites-first (config [agents] favorites, then the remaining kinds). Cost if wrong: one setter signature change.
Task 18: Ruling: SubmitView defines/emits only KeepMsg/CleanMsg (form.SubmitMsg already exists in-package from T16; redeclaring wouldn't compile) — correct reading, accepted.
Task 18: Ruling: account rate-limited/auth-failed marker is text-only ("!" + note), no color, because widgets.Picker has no per-item style hook and the implementer declined to modify a shared widget silently — accepted. If color is wanted later, add a per-item style hook to Picker deliberately.
Task 19: Ruling: RESEQUENCED to run after Task 21, i.e. order becomes 20 -> 20b -> 21 -> 19 -> 22. Plan defect found at dispatch time: T19's amended content ("full Path A/B runs via the real popup; validates the FINISHED code paths end-to-end") requires the app layer from T20/T20b to exist, but the file leaves it positioned before them. T22 still consumes T19's clauth-version finding, so 19 must precede 22. Cost if wrong: none — pure ordering.
Task 20: Ruling: config.Load parse errors refuse startup (beyond the brief's two refusal conditions) — accepted; silently ignoring a malformed config.toml would leave the user's settings mysteriously inert, which is worse than a clear refusal.
Task 20: Ruling: additive form setters (TitleField.SetTitle, PromptField touched-aware SetValue, WorktreeField.SetOn, form.Model.FocusedID) and gitx.FetchPrune accepted — genuine composition gaps between T17/18's field APIs and the app layer, disclosed rather than hidden.
Task 20b: Ruling: submit-block re-focus generalized to Dir/Account (not only Title as spec §9 literally names) — accepted; focusing the field that actually failed is the spec's intent.
Task 20b: Ruling: Esc/⌃C escape hatch from the step-1-failure state accepted — unspecified but fills a real dead end.
Task 20b: Ruling: `Input.TrustRepository` has no config source (spec §9 says "per config" but §12's example omits the key) — DEFERRED as a documented v1 limitation rather than adding an unspecified config surface this late; record in the README (T22) and the final-review triage. Cost if wrong: users needing git safe.directory hit a confusing git error until the key exists.
Task 21: Ruling: PlacementField chip IDs renamed "tab"/"split" -> "tab-here"/"split-here" — accepted; controller verified the rename is coherent across field_placement.go, app.go's placementFromConfigValue, and the config vocabulary, so T20b's `default_placement` pre-selection still resolves. Cost if wrong: none (verified).
Task 21: Ruling: 50ms sleep in zone tests accepted — inherent to bubblezone's async Scan/Get, test-only, not a production path. This is the ONLY sanctioned real sleep in the suite.
Task 19: Ruling: both defects go to ONE fix round dispatched to the Task 19 agent (it holds the live reproductions and a working disposable-session method, so it can re-validate live after fixing) rather than a fresh implementer. Cost if wrong: none.
Task 19: Ruling: commit 0abb61e (docs-only, spec §17 ticks) is MISSING the required `Co-Authored-By: Claude Fable 5` trailer — a Global Constraints violation. Accepted rather than corrected: fixing it means rewriting 0abb61e and cf6a8a6, invalidating every SHA this ledger records as its post-compaction recovery map, for an attribution line on a docs commit with no functional content. Surfaced to Zvi in the final report so he can amend during rebase/squash if he wants it. Cost if wrong: one commit in history lacks the trailer.
FINAL REVIEW TRIAGE — Ruling: ONE fix wave (per skill: no second wave) covering C1, I1, I2, I4, I5, I6, the tilde half of I3, plus cheap M4/M6. Rationale: each is a defect a maintainer would be unhappy to discover, each has an existing architectural hook, and all sit in the app/form/plan layers.
FINAL REVIEW TRIAGE — Ruling: I3's FULL path-browsing mode (os.ReadDir browse listing, spec §6 field 2) is PARKED as a documented v1 limitation, not built in the fix wave. It is a genuine feature (debounced directory listing + its own async source + tests), not a repair, and bolting it on in a no-second-wave fix pass is how you ship a half-built feature. The tilde-expansion bug IS fixed (it silently creates a workspace rooted at a literal `~`). Cost if wrong: typing a path fragment ranks existing candidates instead of browsing the filesystem until a follow-up adds it.
FINAL REVIEW TRIAGE — Ruling: remaining minors (M1,M2,M3,M5,M7-M12) parked. Each is polish, disclosed in code/README, or bounded; none blocks real use. M12 (missing trailer on 0abb61e) already ruled.
FINAL FIX WAVE: Ruling: reflow-on-focus (spec §6's per-section "constant height" no longer holds) ACCEPTED — at 80x24 a fixed layout gives ~2 rows/field, so no picker could ever show a candidate, breaking 5 of 10 fields. Re-reviewer measured the UX: moving focus shifts the target 0 to -6 rows at h=24, ALWAYS upward or unchanged, with footer and Create pinned at h-2/h-1. SPEC AMENDMENT CANDIDATE for Zvi: §6 should record the popup-budget reality rather than Atrium's full-height assumption. Cost if wrong: a spec/implementation mismatch on paper.
FINAL FIX WAVE: Ruling: unfocused pickers still reserving blank rows in TALL windows accepted (cosmetic, ~12 rows at 120x40, reclaimed at every constrained size); collapsing unconditionally would have shifted a protected golden frame. Follow-up ticket, not a blocker.
FINAL FIX WAVE: Ruling: I4 resolving the HEAD sentinel at check time rather than creation time accepted — the window is seconds, and worktree-local commits stay unreachable from a moved HEAD, so the case that matters (real work in the worktree) is still correctly refused.
